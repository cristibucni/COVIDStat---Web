import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#3a517b',
    },
    secondary: {
      main: '#8de386',
    },
    text: {
      primary: '#334b49',
      secondary: '#ede9d0',
    },
  },
});

export default theme;
